const path = require("path");
const backendPath = "../../backend/views";
const frontendPath = "./public";
module.exports = {
	// outputDir: "views",
	// chainWebpack: (config) => {
	// 	config.resolve.alias.set("@", path.resolve(__dirname, "./src"));
	// },
};
